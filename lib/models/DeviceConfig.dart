class DeviceConfig{
  late int deviceId;
  late int dsf;
  late int maxTemp;
  late int maxHum;

  DeviceConfig({
  required this.deviceId,
  required this.dsf,
  required this.maxTemp,
  required this.maxHum
});
}